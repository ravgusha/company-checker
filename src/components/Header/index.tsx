import HamburgerIcon from '../HamburgerIcon';
import Logo from '../Logo';

import './style.scss';

const Header = () => {
  return (
    <header>
      <Logo />
      <HamburgerIcon />
    </header>
  );
};

export default Header;
