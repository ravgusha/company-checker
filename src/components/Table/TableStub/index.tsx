import { MESSAGES } from '../../../messages';

import './style.scss';

const TableStub = () => {
  return (
    <div className="stub">
        <div className='stub-image'></div>
      <p>{MESSAGES.NO_COMPANIES}</p>
    </div>
  );
};

export default TableStub;
