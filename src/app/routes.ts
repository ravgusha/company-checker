import AddPage from '../containers/AddPage';
import HomePage from '../containers/HomePage';

const routes = [
  {
    path: '/',
    component: HomePage,
  },
  {
    path: '/add',
    component: AddPage,
  },
];

export default routes;
